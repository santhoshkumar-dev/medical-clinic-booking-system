import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Instrumentation is enabled by default in Next.js 16
  // instrumentation.ts will run on server startup
};

export default nextConfig;
